export class Page {
  key: string;
  label: string;
  progress: number;
  //active- signals that the page should be visible
  active: boolean;
  //active - signals that the page should be clickable
  clickable: boolean;
}
